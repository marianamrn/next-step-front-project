# 🧪 Інструкції для тестування API Платежів

## Швидкий старт

### 1. Відкрийте тестовий файл
```bash
# Відкрийте файл в браузері
open test-payment-flow.html
```

### 2. Авторизація
Перед тестуванням встановіть токен авторизації:
```javascript
// В консолі браузера
localStorage.setItem('token', 'ваш_bearer_токен');
```

## 📋 Послідовність тестування

### Крок 1: Ініціалізація платежу
1. Введіть ID курсу (наприклад, `1`)
2. Натисніть "Ініціювати платіж"
3. Перевірте відповідь - має містити `payment_id`

### Крок 2: Перевірка статусу
1. Скопіюйте `payment_id` з попереднього кроку
2. Вставте в поле "Payment ID"
3. Натисніть "Перевірити статус"
4. Статус має бути `pending`

### Крок 3: Адмін підтвердження
1. Вставте `payment_id` в поле "Payment ID"
2. Натисніть "Підтвердити платіж"
3. Перевірте, що статус змінився на `completed`

### Крок 4: Перевірка доступу
1. Введіть ID курсу
2. Натисніть "Перевірити доступ"
3. `has_access` має бути `true`

### Крок 5: Мої курси
1. Натисніть "Отримати мої курси"
2. Перевірте, що курс з'явився в списку

## 🔄 Автоматичне тестування

### Повний тест
Натисніть кнопку "🚀 Запустити повний тест" для автоматичного виконання всіх кроків.

### Авто-перевірка статусу
1. Введіть `payment_id`
2. Натисніть "Авто-перевірка (30с)"
3. Статус буде перевірятися кожні 30 секунд

## 🎯 Тестування різних сценаріїв

### Сценарій 1: Успішна оплата
1. Ініціюйте платіж
2. Підтвердіть як адмін
3. Перевірте доступ
4. Перевірте "Мої курси"

### Сценарій 2: Безкоштовний курс
1. Введіть ID безкоштовного курсу
2. Натисніть "Записатися на безкоштовний курс"
3. Перевірте доступ

### Сценарій 3: Невдалий платіж
1. Ініціюйте платіж
2. Натисніть "Обробити невдалий платіж"
3. Перевірте статус

### Сценарій 4: Адмін панель
1. Натисніть "Отримати всі платежі"
2. Перевірте фільтри за статусом
3. Тестуйте підтвердження/відхилення

## 🚨 Перевірка помилок

### Помилка 401 (Unauthorized)
- Перевірте токен в localStorage
- Переконайтеся, що користувач залогінений

### Помилка 404 (Not Found)
- Перевірте правильність ID
- Переконайтеся, що ресурс існує

### Помилка 422 (Validation Error)
- Перевірте валідність даних
- Перегляньте повідомлення про помилки

## 📊 Очікувані результати

### Успішна відповідь ініціалізації
```json
{
  "payment": {
    "id": 1,
    "amount": 1500,
    "payment_status": "pending"
  },
  "liqpay_data": {
    "url": "https://www.liqpay.ua/api/3/checkout",
    "params": {
      "data": "...",
      "signature": "..."
    }
  }
}
```

### Успішна відповідь статусу
```json
{
  "payment": {
    "id": 1,
    "payment_status": "completed",
    "amount": 1500
  }
}
```

### Успішна відповідь доступу
```json
{
  "has_access": true,
  "enrollment": {
    "id": 1,
    "status": "active"
  }
}
```

## 🔧 Діагностика

### Консоль браузера
Відкрийте DevTools (F12) і перегляньте:
- Network tab для HTTP запитів
- Console tab для помилок JavaScript

### Логи API
Перевірте відповіді API на наявність:
- Правильних HTTP статусів
- Валідного JSON
- Повідомлень про помилки

## 📞 Підтримка

При виникненні проблем:
1. Перевірте консоль браузера
2. Використайте Network tab для аналізу запитів
3. Перевірте токен авторизації
4. Зверніться до команди розробки

---

**Примітка:** Цей файл призначений для швидкого тестування API. Для повного тестування використовуйте автоматизовані тести. 